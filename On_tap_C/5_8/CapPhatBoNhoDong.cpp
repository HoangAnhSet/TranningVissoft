#include <iostream>
using namespace std;

int main()
{   /*  //Khai Báo Bộ Nhớ
    double *contro = NULL; // con tro duoc khoi tao voi gia tri null
    contro = new double;   // yeu cau bo nho cho bien

    *contro = 1234.56; // luu giu gia tri tai dia chi da duoc cap phat
    cout << "Gia tri cua contro la: " << *contro << endl;

    delete contro; // giai phong bo nho.

    cout << "Hello";
    return 0; */
    /*    //Cấp phát bộ nhớ động cho mảng C++
    int *ptr=NULL;
    ptr=new int[3];
    delete ptr; */
    //Cấp phát bộ nhớ cho mảng hai chiều
    /*  // Kết hớp toán tử * và toán tử ++
   int arr[3]={1,2,3};
   int *ptr;
   ptr=arr;
   int i=0;
//    printf("%d\n",*ptr++);
//    printf("%d\n",*(ptr++));
    printf("%d\n",*(++ptr)); */
   /*  //Một số kiểu express
    int arr[3] = {1, 2, 3};
    int *ptr;
    ptr = arr;
   /*  *ptr++;  => lấy giá trị của ptr sau đó tăng địa chỉ ptr lên 1
    (*ptr)++; => lấy giá trị của ptr, sau đó tăng giá trị lên một
    *++ptr; => tăng giá trị địa chỉ của ptr lên 1, sau đó lấy giá trị
    ++*ptr; => tăng giá giá trị của ptr lên 1*/
    // printf("Gia tri dia chi cua arr%x\n",arr);
    // printf("Gia tri ban dau dia chi cua ptr%x\n",ptr);
    // printf("%d",(*ptr)++);
    // printf("%d",(*ptr));
    // printf("%d",*++ptr);
    //  printf("%d",++*ptr); */

   /*  //Quyền ưu tiên
    // int a=1;
    // int b=2;
    // int c=a++ + ++b; //=> giá trị của c=4, sau đó giá trị của a tăng lên 1
    // int d=++a + ++b; //=> giá trị của a tăng 1, giá trị của b tăng 1,giá trị của d=5
    // int k=++a + b++; // => giá trị a tăng 1, giá trị của k=4, sau đó giá trị của b tăng 1
    // int e=a++ + b++; // => giá trị của e=3, sau đó tăng giá trị a lên 1, tăng giá trị b lên 1
    // printf("%d",c);
    // printf("%d",d);
    // printf("%d",k);
    // printf("%d",e); 
    */


}
